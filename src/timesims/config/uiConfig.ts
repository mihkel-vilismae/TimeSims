// Centralized UI tuning defaults for TimeSims UX.

export const DEFAULT_PREPARE_INPUT_SEC = 10;

// Context menu sizing is used for clamping to viewport.
// Keep in sync with CSS (context-menu width/padding/typography).
export const DEFAULT_CONTEXT_MENU_SIZE_PX = { w: 220, h: 180 } as const;
